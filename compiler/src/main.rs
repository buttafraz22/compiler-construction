pub mod lexer;

fn main() {
    // let source_code = "hindsa x = 10; asharia y = 20.5; agar (x == y) { niklo; } irshaad "10"; wapsi 0;".to_string();
    // let mut lexer = lexer::LexicalAnalyzer::new(source_code.clone());
    // let tokens = lexer.tokenize();
    // println!("\n\n\nSource Code: {} \n\n\n", source_code);
    // lexer::print_tokens(&tokens);
}
